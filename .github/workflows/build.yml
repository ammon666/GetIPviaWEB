name: Build GetIPviaWEB Service
on:
  push:
    branches: [ main ]
  workflow_dispatch: # 支持手动触发

jobs:
  build-windows:
    runs-on: windows-latest
    defaults:
      run:
        shell: pwsh
        working-directory: .
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        id: checkout

      - name: 设置 Go 环境
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'
          cache: true
        id: setup-go

      - name: 验证 GitHub Secrets 是否配置
        run: |
          Set-StrictMode -Off
          $ErrorActionPreference = 'Continue'
          
          Write-Host "🔍 检查IP_REPORT_API_KEY配置状态"
          if ("${{ secrets.IP_REPORT_API_KEY }}" -eq "") {
              Write-Host "❌ IP_REPORT_API_KEY 未配置，请在仓库Secrets中添加该密钥"
              exit 1
          } else {
              Write-Host "✅ IP_REPORT_API_KEY 配置正常"
          }
        id: check-secret

      - name: 修复模块校验和并整理依赖
        run: |
          Set-StrictMode -Off
          $ErrorActionPreference = 'Continue'
          
          Write-Host "🔧 清理Go模块缓存并整理依赖"
          go clean -modcache
          go mod tidy
          if ($LASTEXITCODE -ne 0) {
              Write-Host "❌ go mod tidy 执行失败，退出码：$LASTEXITCODE"
              exit 1
          }
          Write-Host "✅ 依赖整理完成"
        id: fix-mod

      - name: 编译可执行文件（注入APIKey）
        run: |
          Set-StrictMode -Off
          $ErrorActionPreference = 'Continue'
          
          Write-Host "🔧 开始编译Go程序（注入APIKey）"
          go build -o GetIPviaWEB.exe -ldflags="-s -w -X main.APIKey=${{ secrets.IP_REPORT_API_KEY }}" main.go
          if ($LASTEXITCODE -ne 0) {
              Write-Host "❌ Go程序编译失败，退出码：$LASTEXITCODE"
              exit 1
          }
          if (Test-Path "GetIPviaWEB.exe") {
              $fileSize = (Get-Item GetIPviaWEB.exe).Length
              Write-Host "✅ 编译成功，文件大小：$fileSize 字节"
          } else {
              Write-Host "❌ 编译失败，未生成GetIPviaWEB.exe"
              exit 1
          }
        id: build-exe

      - name: 安装/验证 Inno Setup
        run: |
          Set-StrictMode -Off
          $ErrorActionPreference = 'Continue'
          
          Write-Host "🔧 检查Inno Setup安装状态"
          $isccExists = $false
          try {
              Get-Command "ISCC.exe" -ErrorAction SilentlyContinue | Out-Null
              $isccExists = $true
          } catch {
              $isccExists = $false
          }

          if (-not $isccExists) {
              Write-Host "🔧 Inno Setup未安装，开始通过Chocolatey安装..."
              choco install innosetup -y --no-progress --force
              if ($LASTEXITCODE -ne 0) {
                  Write-Host "❌ Chocolatey安装Inno Setup失败，退出码：$LASTEXITCODE"
                  exit 1
              }
          } else {
              Write-Host "✅ Inno Setup已预装，跳过安装步骤"
          }

          # 强制执行ISCC验证
          $isccTest = $false
          & ISCC.exe /? 2>&1 | Out-Null
          if ($LASTEXITCODE -eq 0 -or $LASTEXITCODE -eq 1) {
              $isccTest = $true
          }
          
          if ($isccTest) {
              Write-Host "✅ Inno Setup验证成功（可正常调用ISCC编译器）"
              Write-Host "🔍 当前工作目录：$(Get-Location)"
              Write-Host "🔍 ISCC.exe路径：$(Get-Command ISCC.exe | Select-Object -ExpandProperty Source)"
          } else {
              Write-Host "❌ Inno Setup验证失败"
              exit 1
          }
          
          # 强制重置退出码为0
          $global:LASTEXITCODE = 0
          Write-Host "✅ 强制重置退出码为0，流程继续执行"
        id: check-inno

      # ========== 生成修复后的ISS脚本（修复Code段语法错误） ==========
      - name: 生成 Inno Setup 安装包脚本
        run: |
          Set-StrictMode -Off
          $ErrorActionPreference = 'Continue'
          
          Write-Host "🔧 开始生成ISS脚本（修复权限+进程残留+Code段语法）"
          # 清空旧文件
          if (Test-Path "GetIPviaWEB.iss") { Remove-Item "GetIPviaWEB.iss" -Force }
          
          # 逐行写入修复后的ISS内容
          " [Setup]" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Force
          " AppName=GetIPviaWEB Service" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          " AppVersion=1.0.0" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          # 改用用户目录（无需管理员权限）
          " DefaultDirName={userappdata}\GetIPviaWEB" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          " DefaultGroupName=GetIPviaWEB" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          " OutputDir=./installer" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          " OutputBaseFilename=GetIPviaWEB-Setup" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          " Compression=lzma" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          " SolidCompression=yes" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          # 声明管理员权限（兜底）
          " PrivilegesRequired=admin" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          # 禁用不必要的页面，加快安装
          " DisableDirPage=no" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          " DisableProgramGroupPage=no" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          " AllowNoIcons=yes" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          # 强制安装完成后关闭所有相关进程
          " SetupMutex=GetIPviaWEB-InstallMutex" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          " CloseApplications=yes" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          " CloseApplicationsFilter=GetIPviaWEB.exe" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          "" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          " [Files]" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          # Files段保留Permissions（合法位置）
          " Source: ""GetIPviaWEB.exe""; DestDir: ""{app}""; Flags: ignoreversion; Permissions: users-full" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          "" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          " [Icons]" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          " Name: ""{group}\GetIPviaWEB Service""; Filename: ""{app}\GetIPviaWEB.exe""" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          " Name: ""{commondesktop}\GetIPviaWEB Service""; Filename: ""{app}\GetIPviaWEB.exe""; Flags: createonlyiffileexists" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          "" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          " [Run]" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          # 运行程序后不阻塞安装进程
          " Filename: ""{app}\GetIPviaWEB.exe""; Description: ""启动 GetIPviaWEB 服务""; Flags: postinstall skipifsilent nowait" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          "" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          " [UninstallRun]" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          # 卸载时先关闭进程
          " Filename: ""taskkill.exe""; Parameters: ""/F /IM GetIPviaWEB.exe""; Flags: skipifdoesntexist" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          " Filename: ""{app}\GetIPviaWEB.exe""; Parameters: ""/uninstall""; Flags: skipifdoesntexist" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          "" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          " [UninstallDelete]" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          " Type: filesandordirs; Name: ""{app}""" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          # ========== 修复Code段语法错误 ==========
          " [Code]" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          " procedure CurStepChanged(CurStep: TSetupStep);" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          " var" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          "   ResultCode: Integer;" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          "   SetupExeName: String;" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          " begin" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          "   if CurStep = ssPostInstall then" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          "   begin" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          "     // 获取安装程序文件名并强制关闭" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          "     SetupExeName := ExtractFileName(ExpandConstant('{srcexe}'));" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          "     Exec('taskkill.exe', '/F /IM ' + SetupExeName, '', SW_HIDE, ewWaitUntilTerminated, ResultCode);" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          "   end;" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          " end;" | Out-File -FilePath GetIPviaWEB.iss -Encoding UTF8 -Append
          
          # 转换换行符为CRLF
          $content = Get-Content GetIPviaWEB.iss -Raw
          $content -replace "`n", "`r`n" | Set-Content GetIPviaWEB.iss -Encoding UTF8 -Force
          
          Write-Host "✅ ISS脚本生成成功（已修复所有语法错误）"
        id: generate-iss

      # ========== 编译安装包 ==========
      - name: 编译图形化安装包
        run: |
          Set-StrictMode -Off
          $ErrorActionPreference = 'Continue'
          
          Write-Host "🔧 开始编译安装包"
          # 创建输出目录
          New-Item -ItemType Directory -Path ./installer -Force | Out-Null
          
          # 编译安装包
          $issPath = (Resolve-Path GetIPviaWEB.iss).Path
          & ISCC.exe /O"./installer" /F"GetIPviaWEB-Setup" "$issPath" 2>&1
          if ($LASTEXITCODE -ne 0) {
              Write-Host "❌ 安装包编译失败，退出码：$LASTEXITCODE"
              exit 1
          }
          
          # 验证安装包
          $setupFile = "./installer/GetIPviaWEB-Setup.exe"
          if (Test-Path $setupFile) {
              $fileSize = (Get-Item $setupFile).Length
              Write-Host "✅ 安装包编译成功，文件大小：$fileSize 字节"
          } else {
              Write-Host "❌ 未找到安装包文件"
              exit 1
          }
        id: build-installer

      # ========== 上传产物 ==========
      - name: 上传可执行文件
        uses: actions/upload-artifact@v4
        with:
          name: GetIPviaWEB-Windows-Exe
          path: GetIPviaWEB.exe
          retention-days: 7
        id: upload-exe

      - name: 上传安装包
        uses: actions/upload-artifact@v4
        with:
          name: GetIPviaWEB-Windows-Setup
          path: ./installer/GetIPviaWEB-Setup.exe
          retention-days: 7
        id: upload-installer

      - name: 最终确认
        run: |
          Set-StrictMode -Off
          $ErrorActionPreference = 'Continue'
          
          Write-Host "🎉 全流程执行完成！"
          Write-Host "✅ Go程序：$(Test-Path GetIPviaWEB.exe)"
          Write-Host "✅ ISS脚本：$(Test-Path GetIPviaWEB.iss)"
          Write-Host "✅ 安装包：$(Test-Path ./installer/GetIPviaWEB-Setup.exe)"
        id: final-check