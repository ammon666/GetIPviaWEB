name: Build GetIPviaWEB
on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest
    defaults:
      run:
        shell: pwsh
        working-directory: .
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        id: checkout

      - name: 设置 Go 环境
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'
          cache: true
        id: setup-go

      - name: 验证 GitHub Secrets 是否配置
        run: |
          Set-StrictMode -Off
          $ErrorActionPreference = 'Continue'
          
          Write-Host "🔍 检查IP_REPORT_API_KEY配置状态"
          if ("${{ secrets.IP_REPORT_API_KEY }}" -eq "") {
              Write-Host "❌ IP_REPORT_API_KEY 未配置，请在仓库Secrets中添加该密钥"
              exit 1
          } else {
              Write-Host "✅ IP_REPORT_API_KEY 配置正常"
          }
        id: check-secret

      - name: 修复模块校验和并整理依赖
        run: |
          Set-StrictMode -Off
          $ErrorActionPreference = 'Continue'
          
          Write-Host "🔧 清理Go模块缓存并整理依赖"
          go clean -modcache
          go mod tidy
          if ($LASTEXITCODE -ne 0) {
              Write-Host "❌ go mod tidy 执行失败，退出码：$LASTEXITCODE"
              exit 1
          }
          Write-Host "✅ 依赖整理完成"
        id: fix-mod

      - name: 编译可执行文件（注入APIKey）
        run: |
          Set-StrictMode -Off
          $ErrorActionPreference = 'Continue'
          
          Write-Host "🔧 开始编译Go程序（注入APIKey+生成GUI程序）"
          go build -o GetIPviaWEB.exe -ldflags="-s -w -H windowsgui -X main.APIKey=${{ secrets.IP_REPORT_API_KEY }}" main.go
          if ($LASTEXITCODE -ne 0) {
              Write-Host "❌ Go程序编译失败，退出码：$LASTEXITCODE"
              exit 1
          }
          if (Test-Path "GetIPviaWEB.exe") {
              $fileSize = (Get-Item GetIPviaWEB.exe).Length
              Write-Host "✅ 编译成功，文件大小：$fileSize 字节"
          } else {
              Write-Host "❌ 编译失败，未生成GetIPviaWEB.exe"
              exit 1
          }
        id: build-exe

      - name: 安装/验证 Inno Setup
        run: |
          Set-StrictMode -Off
          $ErrorActionPreference = 'Continue'
          
          Write-Host "🔧 检查Inno Setup安装状态"
          $isccExists = $false
          try {
              Get-Command "ISCC.exe" -ErrorAction SilentlyContinue | Out-Null
              $isccExists = $true
          } catch {
              $isccExists = $false
          }

          if (-not $isccExists) {
              Write-Host "🔧 Inno Setup未安装，开始通过Chocolatey安装..."
              choco install innosetup -y --no-progress --force
              if ($LASTEXITCODE -ne 0) {
                  Write-Host "❌ Chocolatey安装Inno Setup失败，退出码：$LASTEXITCODE"
                  exit 1
              }
          } else {
              Write-Host "✅ Inno Setup已预装，跳过安装步骤"
          }

          $isccTest = $false
          & ISCC.exe /? 2>&1 | Out-Null
          if ($LASTEXITCODE -eq 0 -or $LASTEXITCODE -eq 1) {
              $isccTest = $true
          }
          
          if ($isccTest) {
              Write-Host "✅ Inno Setup 6.7.0验证成功"
          } else {
              Write-Host "❌ Inno Setup验证失败"
              exit 1
          }
          
          $global:LASTEXITCODE = 0
          Write-Host "✅ 重置退出码，流程继续"
        id: check-inno

      # ========== 生成ISS脚本（彻底解决YAML语法+Type参数问题） ==========
      - name: 生成 Inno Setup 安装包脚本
        run: |
          Set-StrictMode -Off
          $ErrorActionPreference = 'Continue'
          
          Write-Host "🔧 生成ISS脚本（YAML+ISS双合规）"
          if (Test-Path "GetIPviaWEB.iss") { Remove-Item "GetIPviaWEB.iss" -Force }
          
          # 核心：改用数组逐行定义ISS内容，无Here-String，避免YAML解析冲突
          $issLines = @(
              "[Setup]",
              "AppName=GetIPviaWEB",
              "AppVersion=1.0.0",
              "DefaultDirName={pf}\GetIPviaWEB",
              "DefaultGroupName=GetIPviaWEB",
              "OutputDir=./installer",
              "OutputBaseFilename=GetIPviaWEB-Setup",
              "Compression=lzma",
              "SolidCompression=yes",
              "PrivilegesRequired=admin",
              "SetupMutex=GetIPviaWEB-InstallMutex",
              "CloseApplications=yes",
              "CloseApplicationsFilter=GetIPviaWEB.exe",
              "",
              "[Files]",
              "Source: ""GetIPviaWEB.exe""; DestDir: ""{app}""",
              "",
              "[Icons]",
              "Name: ""{group}\GetIPviaWEB""; Filename: ""{app}\GetIPviaWEB.exe""",
              "Name: ""{group}\卸载GetIPviaWEB""; Filename: ""{uninstallexe}""; Parameters: ""/VERYSILENT""",
              "",
              "[Run]",
              "Filename: ""{app}\GetIPviaWEB.exe""; Parameters: ""install""; Flags: runhidden",
              "",
              "[UninstallRun]",
              "Filename: ""taskkill.exe""; Parameters: ""/F /IM GetIPviaWEB.exe""; Flags: skipifdoesntexist runhidden nowait",
              "Filename: ""{app}\GetIPviaWEB.exe""; Parameters: ""uninstall""; Flags: skipifdoesntexist runhidden nowait",
              "Filename: ""cmd.exe""; Parameters: ""/c rmdir /s /q ""{app}""""; Flags: skipifdoesntexist runhidden nowait",
              "",
              "[UninstallDelete]",
              # 严格合规的Type参数格式：分号+空格，无隐形字符
              "Type: files; Name: ""{app}\*.*""",
              "Type: dirs; Name: ""{app}""",
              "",
              "[Code]",
              "function InitializeSetup(): Boolean;",
              "var",
              "  IsAdmin: Boolean;",
              "begin",
              "  IsAdmin := IsAdminLoggedOn();",
              "  if not IsAdmin then",
              "  begin",
              "    MsgBox('请以管理员身份运行安装包！' + #13#10 + '右键 → 以管理员身份运行', mbError, MB_OK);",
              "    Result := False;",
              "  end",
              "  else",
              "  begin",
              "    Result := True;",
              "  end;",
              "end;"
          )
          
          # 拼接为完整内容（CRLF换行，符合Inno Setup要求）
          $issContent = $issLines -join "`r`n"
          
          # 关键：UTF8无BOM写入，解决Type参数解析错误
          $utf8NoBom = New-Object System.Text.UTF8Encoding $false
          [System.IO.File]::WriteAllText("$PWD/GetIPviaWEB.iss", $issContent, $utf8NoBom)
          
          # 验证文件
          if (Test-Path "GetIPviaWEB.iss") {
              $size = (Get-Item GetIPviaWEB.iss).Length
              Write-Host "✅ ISS脚本生成成功，大小：$size 字节"
              Write-Host "🔍 [UninstallDelete]段内容："
              Get-Content "GetIPviaWEB.iss" | Select-String -Pattern "Type: files|Type: dirs"
          } else {
              Write-Host "❌ ISS脚本生成失败"
              exit 1
          }
        id: generate-iss

      # ========== 编译安装包 ==========
      - name: 编译图形化安装包
        run: |
          Set-StrictMode -Off
          $ErrorActionPreference = 'Continue'
          
          Write-Host "🔧 编译安装包（ISCC 6.7.0 核心命令）"
          New-Item -ItemType Directory -Path ./installer -Force | Out-Null
          
          $issPath = (Resolve-Path GetIPviaWEB.iss).Path
          & ISCC.exe "$issPath" /O"./installer" /F"GetIPviaWEB-Setup" 2>&1
          
          if ($LASTEXITCODE -ne 0) {
              Write-Host "❌ 安装包编译失败，退出码：$LASTEXITCODE"
              exit 1
          }
          
          $setupFile = "./installer/GetIPviaWEB-Setup.exe"
          if (Test-Path $setupFile) {
              $size = (Get-Item $setupFile).Length
              Write-Host "✅ 安装包编译成功！大小：$size 字节"
              Write-Host "✅ 路径：$setupFile"
          } else {
              Write-Host "❌ 未找到安装包文件"
              exit 1
          }
        id: build-installer

      # ========== 上传产物 ==========
      - name: 上传可执行文件
        uses: actions/upload-artifact@v4
        with:
          name: GetIPviaWEB-Windows-Exe
          path: GetIPviaWEB.exe
          retention-days: 7
        id: upload-exe

      - name: 上传安装包
        uses: actions/upload-artifact@v4
        with:
          name: GetIPviaWEB-Windows-Setup
          path: ./installer/GetIPviaWEB-Setup.exe
          retention-days: 7
        id: upload-installer

      - name: 最终确认
        run: |
          Set-StrictMode -Off
          Write-Host "🎉 全流程执行完成！"
          Write-Host "✅ Go程序：$(Test-Path GetIPviaWEB.exe)"
          Write-Host "✅ ISS脚本：$(Test-Path GetIPviaWEB.iss)"
          Write-Host "✅ 安装包：$(Test-Path ./installer/GetIPviaWEB-Setup.exe)"
        id: final-check